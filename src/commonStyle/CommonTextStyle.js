import {StyleSheet} from 'react-native';
/**
 * 공통 문자 스타일을 다루는 js 파일
 *
 */

export const CommonNormalText = () => {
  return StyleSheet.create({
    color: {
      color: 'black',
    },
    labelTitle: {
      fontSize: 20,
    },
  });
};
